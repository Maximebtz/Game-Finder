<div
    class="rounded-lg max-w-80 min-w-60 h-80 min-h-40 flex flex-col justify-center items-center shadow-card p-4">
    <div class="rounded-md w-full h-1/2 overflow-hidden">
        <img src="{{game.thumbnail}}" alt="{{ game.name }} thumbnail" class="w-full h-fit ">
    </div>

    <div class="h-1/2 w-full mt-4">
        <p class="font-bold">
            {{ game.name }}
        </p>
    </div>
    <div class="flex w-full justify-end">
        {{ include('components/btn/ancre.html.twig', {'text': 'Voir plus', 'path': path('game_details', {'id': game.id})}) }}
    </div>
</div>